import 'package:chat_bot/core/app.dart';
import 'package:flutter/material.dart';
import 'package:chat_bot/injection_container.dart' as di;

void main() {
  di.init();
  runApp(MyApp());
}
/// Target -> it should not be a tutorial helll
/// so taskes here
/// first complete the todos
/// add a loading event and state and handle it on ui
/// add a type writing packege make the ui atractive
/// make it responsive ---> use sizer package /
/// imporve desinging
/// Advanced-------(optional)
/// save the chats in backend *(clone gemini)
/// @lets_speedup,,, @mhzhare